{% extends "templates/landing.njk" %}
{% block content %}
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">CMS</h2>
        <ul class="space-y-1">
            {% for name, items in cms %}
                <li>
                    <strong>{{ name }}</strong>
                    {% if items and items.length is not undefined %}
                        <span>({{ items.length }})</span>
                    {% endif %}
                </li>
            {% endfor %}
        </ul>
    </section>
{% endblock %}