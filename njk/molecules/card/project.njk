{% from "molecules/card/card.njk" import render as renderCard %}

{% macro render(project) %}
  {{ renderCard({
    href: project.url,
    title: project.title,
    eyebrow: project.role,
    description: project.summary,
    image: project.thumbnail,
    meta: project.year
  }) }}
{% endmacro %}