# Section Controllers

BaseSection-driven controllers for page sections (Hero, Work, Biography).

- Emit/listen via `AnimationBus`
- Lifecycle: intro/outro/scroll triggers
- Keep in `choreography/` to avoid circular deps
