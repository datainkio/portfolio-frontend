---
description: "Services that turn raw Figma file data into palette, style, and typography token sets."
type: index
---

`FileService` fetches; `PaletteService`, `StyleService`, and `TypographyService` each derive one token family.
