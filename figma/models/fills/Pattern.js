export class Pattern {
    constructor(data) {
        this.blendMode = data.blendMode;
        this.opacity = data.opacity;
        this.type = data.type;
        this.visible = data.visible;
    }
}